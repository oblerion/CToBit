#ifndef io_hpp
#define io_hpp
#include <iostream>
#include <fstream>
#include <sstream>
#include <string>
using namespace std;
string ReadFile(string file);
void WriteFile(string file,string data);
#endif
